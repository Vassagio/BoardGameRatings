@using BoardGameRatings.WebSite.Controllers
@model BoardGameRatings.WebSite.ViewModels.GameTypesViewModel

@{
    ViewData["Title"] = "GameTypes";
}

<div>
    <table class="table table-striped table-bordered table-responsive">
        <thead>
        <tr>
            <th class="head">
                <a class="btn btn-default" href="@Url.RouteUrl(GameTypesController.BuildAddActionRouteValues())">Description</a>
            </th>
            <th></th>
        </tr>
        </thead>
        @foreach (var game in Model.GameTypes)
        {
            <tr>
                <td class="col-md-3">
                    <a href="@Url.RouteUrl(GameTypesController.BuildEditActionRouteValues(game.Id))">@game.Description</a>
                </td>
                <td class="col-md-1 text-center">
                    <a href="@Url.RouteUrl(GameTypesController.BuildRemoveActionRouteValues(game.Id))">
                        <i class="fa fa-trash" style="color: red"></i>
                    </a>
                </td>
            </tr>
        }
    </table>
</div>